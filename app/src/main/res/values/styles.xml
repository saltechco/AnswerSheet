<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!-- Searching Progress Bar -->
    <style name="SearchingProgressBar" parent="Widget.AppCompat.ProgressBar">
        <item name="colorAccent">@color/elements_color_tint</item>
    </style>

    <!--Remove Progress Bar -->
    <style name="RemoveProgressBar" parent="Widget.AppCompat.ProgressBar">
        <item name="colorAccent">@color/error</item>
    </style>

    <!--Loading Progress Bar -->
    <style name="LoadingProgressBar" parent="Widget.AppCompat.ProgressBar">
        <item name="colorAccent">@color/disable_button</item>
    </style>

    <style name="AppTheme.Picker" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:textColorPrimary">@android:color/black</item>
    </style>
</resources>
